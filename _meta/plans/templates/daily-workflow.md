# 📅 毎日のワークフロー

このドキュメントは、Skill改善作業を行う際の毎日のルーティンを定義します。
このワークフローに従うことで、効率的に作業を進め、品質を保つことができます。

---

## 🌅 朝のルーティン (5-10分)

### 1. 環境準備

```bash
# リポジトリに移動
cd /Users/gaku/claude-code-skills

# 最新の状態を取得
git pull

# 現在のブランチを確認
git branch

# 進捗を確認
npm run track
cat PROGRESS.md | grep "In Progress"
```

### 2. 今日のタスク確認

```bash
# 週次計画を確認
cat plans/tracking/weekly-reports/week-XX.md

# 今日やるべきことをリストアップ
echo "## Today's Tasks (YYYY-MM-DD)" > /tmp/today-tasks.md
echo "- [ ] Task 1" >> /tmp/today-tasks.md
echo "- [ ] Task 2" >> /tmp/today-tasks.md
echo "- [ ] Task 3" >> /tmp/today-tasks.md
```

### 3. 作業ディレクトリに移動

```bash
# 今週対象のSkillディレクトリに移動
cd {skill-name}

# 既存の構造を確認
ls -la
tree -L 2
```

---

## 💼 作業中のルーティン

### コード例を書く際のフロー

#### 1. コード例の作成
```bash
# 一時ディレクトリで動作確認
mkdir -p /tmp/{skill-name}-examples
cd /tmp/{skill-name}-examples

# 依存関係のインストール (必要に応じて)
npm init -y
npm install --save-dev typescript jest @types/jest

# コードを書いて動作確認
code example.ts
npx ts-node example.ts

# テストを書いて実行
code example.test.ts
npm test
```

#### 2. 動作確認が取れたらガイドに追加
```bash
# claude-code-skillsに戻る
cd /Users/gaku/claude-code-skills/{skill-name}

# ガイドを開いて編集
code guides/guide-name.md

# コード例を追加 (マークダウンのコードブロック)
```

### ガイドを書く際のフロー

#### 構成の確認
```markdown
# ガイドの基本構成

## 1. 概要 (2,000-3,000 chars)
- 何について説明するか
- なぜ重要か
- 誰が読むべきか

## 2. 基本概念 (3,000-5,000 chars)
- 理論的背景
- 基本用語の定義

## 3. ケーススタディ1 (5,000-10,000 chars)
- プロジェクト概要
- 完全なコード例
- 実行結果

## 4. ケーススタディ2 (5,000-10,000 chars)
- 別のユースケース
- コード例

## 5. よくある失敗 (3,000-5,000 chars)
- 失敗パターン 5-10個
- 各失敗の詳細

## 6. チェックリスト (1,000-2,000 chars)
- 実行時の確認項目
```

### こまめなコミット

```bash
# 変更内容を確認
git status
git diff

# ステージング
git add .

# コミット
git commit -m "feat({skill-name}): add section X to guide Y"

# または safe-commit-push.sh を使用
./scripts/safe-commit-push.sh "feat({skill-name}): add section X to guide Y"
```

---

## ⏰ 定期的な確認 (2-3時間ごと)

### 進捗確認
```bash
# 文字数確認
cd /Users/gaku/claude-code-skills/{skill-name}
find . -name "*.md" -exec wc -c {} + | tail -1

# 今日追加した文字数
git diff HEAD~1 --stat

# 進捗更新
cd ..
npm run track
```

### 休憩時のチェックリスト
- [ ] 水分補給
- [ ] ストレッチ
- [ ] 目を休める
- [ ] 進捗を記録

---

## 🌆 夕方のルーティン (10-15分)

### 1. 今日の成果を確認

```bash
# 今日の変更を確認
cd /Users/gaku/claude-code-skills
git log --since="today" --oneline

# 追加した文字数
git diff HEAD~1 --stat | grep "insertions"

# 進捗を更新
npm run track
cat PROGRESS.md | grep "{skill-name}"
```

### 2. 時間記録

```bash
# 時間記録を追加
cat >> plans/tracking/time-log.md << EOF

## YYYY-MM-DD
- Skill: {skill-name}
- 作業時間: Xh
- 作業内容:
  - Task 1 (Xh)
  - Task 2 (Xh)
- 成果物:
  - guides/guide-name.md (+X,XXX chars)
- メモ:
EOF
```

### 3. 最終コミット

```bash
# 今日の作業をコミット
git add .
./scripts/safe-commit-push.sh "feat({skill-name}): complete day X tasks"

# プッシュ確認
git log -1
git status
```

### 4. 翌日の準備

```bash
# 翌日のタスクを確認
cat plans/tracking/weekly-reports/week-XX.md

# 必要なリソースがあればメモ
echo "## Tomorrow's Preparation" > /tmp/tomorrow-prep.md
echo "- [ ] Resource 1" >> /tmp/tomorrow-prep.md
echo "- [ ] Resource 2" >> /tmp/tomorrow-prep.md
```

---

## 📋 状況別のワークフロー

### ケース1: コード例が動かない

```bash
# 1. エラーメッセージを確認
# 2. 依存関係を確認
npm list

# 3. TypeScriptの型エラーを確認
npx tsc --noEmit

# 4. シンプルな例から試す
# 5. 公式ドキュメントを確認

# 6. 解決したらトラブルシューティングに追加
cat >> references/troubleshooting-guide.md << EOF

### エラー: [エラーメッセージ]

**症状**:
**原因**:
**解決策**:
\`\`\`bash
# 解決コマンド
\`\`\`
EOF
```

### ケース2: ガイドが長くなりすぎた

```bash
# 1. セクションを確認
cat guides/guide-name.md | grep "^##"

# 2. 分割が必要か判断
# 3. 分割する場合は新しいガイドを作成
cp guides/guide-name.md guides/guide-name-part2.md

# 4. 内容を整理
# 5. 相互リンクを追加
```

### ケース3: 時間が足りない

```bash
# 1. 優先度を見直す
# - 必須: ガイドの完成
# - 推奨: チェックリスト・テンプレート
# - 任意: スクリプト

# 2. タスクを調整
# - 重要度の低いタスクを次週に
# - 完成度を80%に設定

# 3. 週次計画を更新
code plans/tracking/weekly-reports/week-XX.md
```

---

## ✅ 毎日の最低限のチェックリスト

### 朝 (必須)
- [ ] `git pull` で最新を取得
- [ ] 今日のタスクを確認
- [ ] 作業ディレクトリに移動

### 作業中 (推奨)
- [ ] こまめにコミット (1-2時間ごと)
- [ ] コード例の動作確認
- [ ] 2-3時間ごとに進捗確認

### 夕方 (必須)
- [ ] 今日の成果を確認
- [ ] 時間記録を更新
- [ ] 最終コミット & プッシュ
- [ ] 翌日のタスク確認

---

## 🎯 効率化のTips

### エディタ設定

#### VS Code推奨拡張機能
- Markdown All in One
- Markdown Preview Enhanced
- Code Spell Checker
- Prettier

#### VS Code設定
```json
{
  "editor.wordWrap": "on",
  "markdown.preview.breaks": true,
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
```

### エイリアス設定

```bash
# ~/.bashrc または ~/.zshrc に追加
alias ccs="cd /Users/gaku/claude-code-skills"
alias ccs-track="cd /Users/gaku/claude-code-skills && npm run track"
alias ccs-commit="cd /Users/gaku/claude-code-skills && ./scripts/safe-commit-push.sh"

# 使用例
ccs                    # リポジトリに移動
ccs-track              # 進捗確認
ccs-commit "message"   # コミット
```

### スニペット

#### Markdownスニペット (VS Code)
```json
{
  "Case Study": {
    "prefix": "case",
    "body": [
      "## ケーススタディ${1:1}: ${2:タイトル}",
      "",
      "### プロジェクト概要",
      "- ${3:説明}",
      "",
      "### 実装",
      "\\`\\`\\`typescript",
      "${4:コード}",
      "\\`\\`\\`",
      "",
      "### 実行結果",
      "${5:結果}"
    ]
  },
  "Failure Pattern": {
    "prefix": "fail",
    "body": [
      "### 失敗${1:1}: ${2:タイトル}",
      "",
      "**症状**",
      "- ${3:症状}",
      "",
      "**原因**",
      "- ${4:原因}",
      "",
      "**解決策**",
      "\\`\\`\\`typescript",
      "${5:解決コード}",
      "\\`\\`\\`",
      "",
      "**予防策**",
      "- ${6:予防策}",
      "",
      "**時間的影響**",
      "- 合計: ${7:X}時間のロス"
    ]
  }
}
```

---

## 🔄 週の途中での調整

### 水曜日レビュー (中間チェック)
```bash
# 週の進捗確認
cat plans/tracking/weekly-reports/week-XX.md

# 文字数確認
cd {skill-name}
find . -name "*.md" -exec wc -c {} + | tail -1

# 目標との差分確認
# 必要に応じてタスク調整
```

---

## 📞 サポート・質問

### 困ったときの対処法
1. `plans/templates/skill-improvement-checklist.md` を確認
2. 他のスキルの実装を参考にする
3. 公式ドキュメントを確認
4. Issue/Discussionで質問

---

**このワークフローに従って、毎日コツコツと進めましょう!**

**最終更新**: 2026-01-01
